# SIC/XE 組譯器系統開發規範 (System Construction Prompt)

本文件記錄了建構此 SIC/XE 組譯器系統的核心技術需求與架構規範。

## 系統核心目標
使用 C++ 開發一個具備兩階段 (Two-Pass) 邏輯的組譯器，需同時支援標準 SIC 指令集與 SIC/XE 擴充架構，並能產生符合系統程式教科書標準格式的 `.obj` 物件檔。

## 技術規格需求

### 一、 資料結構與表格
1. **OPTAB (指令表)**：需區分 SIC 與 SIC/XE 指令集。必須標記指令格式 (Format 1/2/3/4) 及其對應的 Opcode。
2. **SYMTAB (符號表)**：記錄所有 Label 及其對應位址，需支援絕對與相對符號的辨識。
3. **LITTAB (Literal 表)**：記錄程式中的 Literal（如 `=C'EOF'`），並在遇到 `LTORG` 或程式結束時分配位址。
4. **Register State**：追蹤暫存器狀態，特別是 `BASE` 暫存器的值與啟用狀態。

### 二、 第一階段 (Pass 1) 邏輯
1. **位址計算 (LOCCTR)**：從 `START` 指令開始計算位址。
2. **指示字處理**：
    - `ORG`：直接設定位址計數器至指定值。
    - `EQU`：將符號定義為數值或表達式。
    - `LTORG`：將當前未分配的 Literals 分配位址並寫入中間碼。
    - `BYTE`：根據內容動態計算長度（如 `X'F1'` 為 1 byte, `C'EOF'` 為 3 bytes）。
3. **模式偵測**：自動偵測程式是否使用了 XE 特性（如 Format 2/4, `#`, `@` 等）。

### 三、 第二階段 (Pass 2) 邏輯
1. **定址模式計算**：
    - **Format 3 Fallback**：優先嘗試 PC 相對定址 (`-2048~2047`)，失敗後嘗試 Base 相對定址 (`0~4095`)。若皆失敗且非 Format 4 則報錯區域。
    - **定址合法性檢查**：拒絕非法組合（如 Immediate + Indexed）。
2. **物件碼生成**：
    - 支援 `n, i, x, b, p, e` 旗標位元的精確計算。
    - 支援 SIC 標準 15 位元絕對定址格式。

### 四、 物件檔格式規範
1. **H Record**：程式名稱需右側補空白至 6 位元。
2. **T Record**：每筆上限為 30 bytes (60 hex chars)。
3. **中斷處理**：遇到 `RESW`, `RESB` 指令時，必須中斷當前 `T` record，待有新機器碼時再開啟新紀錄。
4. **M Record**：針對 Format 4 且涉及外部符號/重定位的指令產生修正紀錄。

### 五、 錯誤處理機制
系統應具備統一的報錯機制，包含但不限於：
- 重複定義的 Label。
- 未定義的符號 (Undefined symbols)。
- 指令助記碼錯誤。
- 位移越界 (Displacement out of range)。
- 語法錯誤（欄位缺失、非法字元）。
